var question = document.getElementById(<%= @question_id %>);
var button = document.getElementById('btn_<%= @question_id %>');
var radios = question.querySelectorAll('input');

<% if @correct %>
    question.className = 'card bg-success text-white';

    radios.forEach(function(radio){
        radio.disabled = true;
    });

    button.style.display = "none";
<% else %>
    question.className = 'card bg-danger text-white';
    button.className = "btn btn-outline-light";
<% end %>


